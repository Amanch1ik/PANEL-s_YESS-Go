services:
<<<<<<< HEAD
  postgres:
    image: postgres:15
    container_name: yess-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: yess_user
      POSTGRES_PASSWORD: secure_password
      POSTGRES_DB: yess_db
    ports:
      - "5432:5432"
    volumes:
      - yess_pg_data:/var/lib/postgresql/data
    networks:
      - yess-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U yess_user"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: yess-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - yess-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  csharp-backend:
    build:
      context: ./YessBackend-master
      dockerfile: Dockerfile
    container_name: yess-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8000
      - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=yess_db;User Id=yess_user;Password=secure_password
      - Redis__ConnectionString=redis:6379
      - Jwt__SecretKey=GENERATE_LONG_RANDOM_SECRET_KEY_HERE_MINIMUM_32_CHARS
      - Jwt__Issuer=yess-loyalty
      - Jwt__Audience=yess-loyalty
      - Jwt__AccessTokenExpireMinutes=60
      - Jwt__RefreshTokenExpireDays=7
    networks:
      - yess-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - yess_uploads:/app/uploads

  admin-panel:
    build:
      context: ./panels-ts-v2
      dockerfile: admin-panel/Dockerfile
    container_name: yess-admin-panel
    restart: unless-stopped
    ports:
      - "8083:80"
    depends_on:
      csharp-backend:
        condition: service_started
    networks:
      - yess-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    environment:
      - NODE_ENV=production

  partner-panel:
    build:
      context: ./panels-ts-v2
      dockerfile: partner-panel/Dockerfile
    container_name: yess-partner-panel
    restart: unless-stopped
    ports:
      - "8081:80"
    depends_on:
      csharp-backend:
        condition: service_started
    networks:
      - yess-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    environment:
      - NODE_ENV=production

volumes:
  yess_pg_data:
  yess_uploads:

networks:
  yess-network:
    driver: bridge

=======
  backend:
    build: 
      context: ./yess-backend
      dockerfile: Dockerfile.dev
    ports:
      - "8000:8000"
    volumes:
      - ./yess-backend:/app
    environment:
      - DATABASE_URL=${DATABASE_URL:-postgresql://yess_user:${POSTGRES_PASSWORD:-dev_password}@postgres:5432/yess_db}
      - ENVIRONMENT=${ENVIRONMENT:-development}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379/0}
      - SECRET_KEY=${SECRET_KEY:-dev-secret-key-change-in-production}
    depends_on:
      - postgres
      - redis
    env_file:
      - .env

  postgres:
    image: postgres:13
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-yess_db}
      - POSTGRES_USER=${POSTGRES_USER:-yess_user}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-dev_password}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    environment:
      - PGADMIN_DEFAULT_EMAIL=${PGADMIN_EMAIL:-admin@yessloyalty.com}
      - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_PASSWORD:-admin}
    ports:
      - "5050:80"
    depends_on:
      - postgres

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

volumes:
  postgres_data:
  redis_data:
>>>>>>> fbeca546db7463a04490b22dcaacce1d07a7fa06
